export type LatLng = {
  x: string
  y: string
  flagged?: boolean
}

export type nonFlaggedLatLng = {
  x: string
  y: string
}

export type HotPlace = {
  x: string
  y: string
  id: number
  flagged?: boolean
}